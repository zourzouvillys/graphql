rootProject.name = 'graphql'


[
 'annotations',
 'core',
 'runtime',
 'schema',
 'validator',
 'generator',
 'jackson',
 'server-netty'
 ].each { _ ->


  include 'graphql-' + _
  project(':graphql-' + _).projectDir = file('graphql-' + _)

}

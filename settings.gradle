rootProject.name = 'graphql'


[
 'annotations',
 'core',
 'runtime',
 'generator',
 'schema',
 'jackson',
 'netty'
 ].each { _ ->


  include 'graphql-' + _
  project(':graphql-' + _).projectDir = file('graphql-' + _)

}

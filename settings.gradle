rootProject.name = 'graphql'


[
 'annotations',
 'core',
 'runtime',
 'schema',
 'validator',
 'generator',
 'jackson',
 'server-netty',
 'client-grpc',
 'server-grpc'
 ].each { _ ->


  include 'graphql-' + _
  project(':graphql-' + _).projectDir = file('graphql-' + _)

}

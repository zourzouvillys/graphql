/**
 * Type declarations.
 * 
 * These are the concrete declarations. They are registered with the type registry.
 * 
 * The types are referenced by way of using a GQLTypeReference. There are 3 implementations: NonNullModifier, ListModifier, and NamedType.
 * NamedType may only point to a declaration registered with the type reference.
 * 
 * @author theo
 *
 */

package io.joss.graphql.core.decl;